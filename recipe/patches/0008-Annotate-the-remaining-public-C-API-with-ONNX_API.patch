From 95f24e674088853c9e94b19152894c03fc124f67 Mon Sep 17 00:00:00 2001
From: Mark Harfouche <mark.harfouche@gmail.com>
Date: Sun, 13 Sep 2026 18:35:34 -0400
Subject: [PATCH] Annotate the remaining public C++ API with ONNX_API

With the shared ONNX libraries built with hidden visibility, and on
Windows exporting only ONNX_API symbols, separate consumers failed to
link against public helpers that were not annotated.

onnx_cpp2py_export needed the checker free functions, the inliner entry
points, OpSchema::Verify, the shape-inference helpers in
shape_inference/implementation.h and the proto streaming operators in
printer.h.

onnxruntime additionally needs (undefined references when linking
libonnxruntime, onnx_test_runner, onnxruntime_test_all and
onnxruntime_provider_test on linux, osx and Windows):

- the OpSchema::Attr overloads that take a default value, for every type
  instantiated by ATTR_SETTER_WITH_DEFAULT_VALUE plus const char*;
- OpSchemaRegistry::loaded_schema_version, read by inline accessors;
- ReplaceAll;
- the public DataTypeUtils static helpers;
- MatMulShapeInference, QLinearMatMulShapeInference and QLinearMatMulDoc;
- the mergeInShapeInfo and UnionShapeInfo overloads;
- both shape_inference::InferShapeForFunctionNode overloads;
- the ParseData template, whose instantiations live in the library.

The ToTensor and ParseData specializations are defined in
tensor_proto_util.cc and tensor_util.cc. GCC and clang give an explicit
specialization the visibility of its primary template, but MSVC only
exports it when the definition itself is dllexport, so onnx.dll exported
none of them and onnxruntime_conda.dll failed to link against
ParseData<int>, ParseData<int64_t> and ToTensor<int64_t>. Annotate the
definitions, and the ParseData(const Tensor*) declaration, with ONNX_API.

The exception classes InferenceError, SchemaError, ValidationError and
ConvertError are exported as whole classes too. They are defined inline
in the headers, so with hidden visibility every library gets its own
local typeinfo. GCC's runtime matches exception types by name, but on
macOS a `catch (const onnx::InferenceError&)` in onnxruntime does not
match an InferenceError thrown inside libonnx. Shape-inference errors that
onnxruntime tolerates or rewraps then escape: on osx-arm64, 27 of
onnxruntime's own unit tests failed, e.g. models that should load with a
shape warning fail with "[ShapeInferenceError] Mismatch between number of
inferred and declared dimensions". SchemaError's member annotations move
to the class.

GraphInferencerImpl is exported as a whole class so its vtable is
available to onnx_cpp2py_export, which instantiates it; MSVC forbids
dllexport on members of a dllexport class, so doInferencing loses its
own annotation.
---
 onnx/checker.h                        | 26 ++++++++++++-------------
 onnx/common/ir_pb_converter.h         |  2 +-
 onnx/defs/data_type_utils.h           |  8 ++++----
 onnx/defs/math/utils.h                |  4 ++--
 onnx/defs/printer.h                   | 28 +++++++++++++--------------
 onnx/defs/schema.h                    | 20 +++++++++----------
 onnx/defs/shape_inference.h           | 14 +++++++-------
 onnx/defs/tensor_proto_util.cc        | 10 +++++-----
 onnx/defs/tensor_proto_util.h         |  2 +-
 onnx/defs/tensor_util.cc              |  2 +-
 onnx/defs/tensor_util.h               |  2 +-
 onnx/inliner/inliner.h                | 10 +++++-----
 onnx/shape_inference/implementation.h |  8 ++++----
 13 files changed, 68 insertions(+), 68 deletions(-)

diff --git a/onnx/checker.h b/onnx/checker.h
index fe88cee..63ac71e 100644
--- a/onnx/checker.h
+++ b/onnx/checker.h
@@ -18,7 +18,7 @@
 namespace ONNX_NAMESPACE::checker {
 // std::string member means copy may throw when allocation fails
 // NOLINTNEXTLINE(bugprone-exception-copy-constructor-throws)
-class ValidationError final : public std::runtime_error {
+class ONNX_API ValidationError final : public std::runtime_error {
  public:
   using std::runtime_error::runtime_error;
   const char* what() const noexcept override {
@@ -145,16 +145,16 @@ class LexicalScopeContext final {
 };
 
 using IR_VERSION_TYPE = decltype(Version::IR_VERSION);
-void check_value_info(const ValueInfoProto& value_info, const CheckerContext& /*ctx*/);
-void check_tensor(const TensorProto& tensor, const CheckerContext& /*ctx*/);
-void check_sparse_tensor(const SparseTensorProto& sparse_tensor, const CheckerContext& /*ctx*/);
-void check_sequence(const SequenceProto& sequence, const CheckerContext& /*ctx*/);
-void check_map(const MapProto& map, const CheckerContext& /*ctx*/);
-void check_optional(const OptionalProto& opt, const CheckerContext& /*ctx*/);
-void check_attribute(const AttributeProto& attr, const CheckerContext& /*ctx*/, const LexicalScopeContext& /*lex_ctx*/);
-void check_node(const NodeProto& node, const CheckerContext& /*ctx*/, const LexicalScopeContext& /*lex_ctx*/);
-void check_graph(const GraphProto& graph, const CheckerContext& /*ctx*/, const LexicalScopeContext& /*parent_lex*/);
-void check_function(
+ONNX_API void check_value_info(const ValueInfoProto& value_info, const CheckerContext& /*ctx*/);
+ONNX_API void check_tensor(const TensorProto& tensor, const CheckerContext& /*ctx*/);
+ONNX_API void check_sparse_tensor(const SparseTensorProto& sparse_tensor, const CheckerContext& /*ctx*/);
+ONNX_API void check_sequence(const SequenceProto& sequence, const CheckerContext& /*ctx*/);
+ONNX_API void check_map(const MapProto& map, const CheckerContext& /*ctx*/);
+ONNX_API void check_optional(const OptionalProto& opt, const CheckerContext& /*ctx*/);
+ONNX_API void check_attribute(const AttributeProto& attr, const CheckerContext& /*ctx*/, const LexicalScopeContext& /*lex_ctx*/);
+ONNX_API void check_node(const NodeProto& node, const CheckerContext& /*ctx*/, const LexicalScopeContext& /*lex_ctx*/);
+ONNX_API void check_graph(const GraphProto& graph, const CheckerContext& /*ctx*/, const LexicalScopeContext& /*parent_lex*/);
+ONNX_API void check_function(
     const FunctionProto& function,
     const CheckerContext& /*ctx*/,
     const LexicalScopeContext& /*parent_lex*/);
@@ -186,13 +186,13 @@ ONNX_API void check_model(
     bool full_check = false,
     bool skip_opset_compatibility_check = false,
     bool check_custom_domain = false);
-std::filesystem::path resolve_external_data_location(
+ONNX_API std::filesystem::path resolve_external_data_location(
     const std::string& base_dir,
     const std::string& location,
     const std::string& tensor_name);
 // Returns a CRT file descriptor on all platforms.
 // The caller owns the fd and must close it.
-int64_t open_external_data(
+ONNX_API int64_t open_external_data(
     const std::string& base_dir,
     const std::string& location,
     const std::string& tensor_name,
diff --git a/onnx/common/ir_pb_converter.h b/onnx/common/ir_pb_converter.h
index 177d445..608166b 100644
--- a/onnx/common/ir_pb_converter.h
+++ b/onnx/common/ir_pb_converter.h
@@ -14,7 +14,7 @@
 
 namespace ONNX_NAMESPACE {
 
-class ConvertError final : public std::runtime_error {
+class ONNX_API ConvertError final : public std::runtime_error {
  public:
   using std::runtime_error::runtime_error;
 
diff --git a/onnx/defs/data_type_utils.h b/onnx/defs/data_type_utils.h
index 126cdb4..12d2583 100644
--- a/onnx/defs/data_type_utils.h
+++ b/onnx/defs/data_type_utils.h
@@ -38,16 +38,16 @@ class DataTypeUtils final {
  public:
   // If the DataType input is invalid, this function will throw std::invalid_argument exception.
   // If ONNX_NO_EXCEPTIONS is set it will abort.
-  static DataType ToType(const std::string& type_str);
+  ONNX_API static DataType ToType(const std::string& type_str);
 
   // If the DataType input is invalid, this function will throw std::invalid_argument exception.
   // If ONNX_NO_EXCEPTIONS is set it will abort.
-  static DataType ToType(const TypeProto& type_proto);
+  ONNX_API static DataType ToType(const TypeProto& type_proto);
 
   // If the DataType input is invalid, this function will throw std::invalid_argument exception.
   // If ONNX_NO_EXCEPTIONS is set it will abort.
-  static const TypeProto& ToTypeProto(const DataType& data_type);
-  static std::string ToDataTypeString(int32_t tensor_data_type);
+  ONNX_API static const TypeProto& ToTypeProto(const DataType& data_type);
+  ONNX_API static std::string ToDataTypeString(int32_t tensor_data_type);
 
  private:
   static void FromString(const std::string& type_str, TypeProto& type_proto);
diff --git a/onnx/defs/math/utils.h b/onnx/defs/math/utils.h
index d3cb30a..c2b371d 100644
--- a/onnx/defs/math/utils.h
+++ b/onnx/defs/math/utils.h
@@ -46,9 +46,9 @@ T GetScalarValueFromTensor(const ONNX_NAMESPACE::TensorProto* t) {
   }
 }
 
-void MatMulShapeInference(ONNX_NAMESPACE::InferenceContext& ctx, int input1Idx, int input2Idx);
+ONNX_API void MatMulShapeInference(ONNX_NAMESPACE::InferenceContext& ctx, int input1Idx, int input2Idx);
 
-void QLinearMatMulShapeInference(ONNX_NAMESPACE::InferenceContext& ctx);
+ONNX_API void QLinearMatMulShapeInference(ONNX_NAMESPACE::InferenceContext& ctx);
 
 int64_t MathOpTwoIntegers(const std::string& op_type, int64_t a, int64_t b);
 
diff --git a/onnx/defs/printer.h b/onnx/defs/printer.h
index 98d695f..7d225c1 100644
--- a/onnx/defs/printer.h
+++ b/onnx/defs/printer.h
@@ -12,33 +12,33 @@
 
 namespace ONNX_NAMESPACE {
 
-std::ostream& operator<<(std::ostream& os, const TensorShapeProto_Dimension& dim);
+ONNX_API std::ostream& operator<<(std::ostream& os, const TensorShapeProto_Dimension& dim);
 
-std::ostream& operator<<(std::ostream& os, const TensorShapeProto& shape);
+ONNX_API std::ostream& operator<<(std::ostream& os, const TensorShapeProto& shape);
 
-std::ostream& operator<<(std::ostream& os, const TypeProto_Tensor& tensortype);
+ONNX_API std::ostream& operator<<(std::ostream& os, const TypeProto_Tensor& tensortype);
 
-std::ostream& operator<<(std::ostream& os, const TypeProto& type);
+ONNX_API std::ostream& operator<<(std::ostream& os, const TypeProto& type);
 
-std::ostream& operator<<(std::ostream& os, const TensorProto& tensor);
+ONNX_API std::ostream& operator<<(std::ostream& os, const TensorProto& tensor);
 
-std::ostream& operator<<(std::ostream& os, const ValueInfoProto& value_info);
+ONNX_API std::ostream& operator<<(std::ostream& os, const ValueInfoProto& value_info);
 
-std::ostream& operator<<(std::ostream& os, const ValueInfoList& vilist);
+ONNX_API std::ostream& operator<<(std::ostream& os, const ValueInfoList& vilist);
 
-std::ostream& operator<<(std::ostream& os, const AttributeProto& attr);
+ONNX_API std::ostream& operator<<(std::ostream& os, const AttributeProto& attr);
 
-std::ostream& operator<<(std::ostream& os, const AttrList& attrlist);
+ONNX_API std::ostream& operator<<(std::ostream& os, const AttrList& attrlist);
 
-std::ostream& operator<<(std::ostream& os, const NodeProto& node);
+ONNX_API std::ostream& operator<<(std::ostream& os, const NodeProto& node);
 
-std::ostream& operator<<(std::ostream& os, const NodeList& nodelist);
+ONNX_API std::ostream& operator<<(std::ostream& os, const NodeList& nodelist);
 
-std::ostream& operator<<(std::ostream& os, const GraphProto& graph);
+ONNX_API std::ostream& operator<<(std::ostream& os, const GraphProto& graph);
 
-std::ostream& operator<<(std::ostream& os, const FunctionProto& fn);
+ONNX_API std::ostream& operator<<(std::ostream& os, const FunctionProto& fn);
 
-std::ostream& operator<<(std::ostream& os, const ModelProto& model);
+ONNX_API std::ostream& operator<<(std::ostream& os, const ModelProto& model);
 
 template <typename ProtoType>
 std::string ProtoToString(const ProtoType& proto) {
diff --git a/onnx/defs/schema.h b/onnx/defs/schema.h
index 2d12374..bd59d07 100644
--- a/onnx/defs/schema.h
+++ b/onnx/defs/schema.h
@@ -94,20 +94,20 @@ class OpSchema;
 using ContextDependentFunctionBodyBuilder =
     std::function<bool(const FunctionBodyBuildContext&, const OpSchema&, FunctionProto&)>;
 
-class SchemaError final : public std::runtime_error {
+class ONNX_API SchemaError final : public std::runtime_error {
  public:
   using std::runtime_error::runtime_error;
 
   explicit SchemaError(const std::string& message) : std::runtime_error(message) {}
 
-  ONNX_API const char* what() const noexcept override {
+  const char* what() const noexcept override {
     if (!expanded_message_.empty()) {
       return expanded_message_.c_str();
     }
     return std::runtime_error::what();
   }
 
-  ONNX_API void AppendContext(const std::string& context) {
+  void AppendContext(const std::string& context) {
     expanded_message_ = ONNX_NAMESPACE::MakeString(std::runtime_error::what(), "\n\n==> Context: ", context);
   }
 
@@ -331,7 +331,7 @@ class OpSchema final {
    * @brief Verifies if a NodeProto matches the pattern specified in
    * the schema.
    */
-  void Verify(const NodeProto& node) const;
+  ONNX_API void Verify(const NodeProto& node) const;
 
   // Functions to set the property of the operator schemas.
   // Sets the number of inputs, either a fixed number or a min and a max.
@@ -446,12 +446,12 @@ class OpSchema final {
 
 // Register "optional" attribute with default value.
 #define ATTR_SETTER_WITH_DEFAULT_VALUE(TypeName)                                                                    \
-  OpSchema& Attr(                                                                                                   \
+  ONNX_API OpSchema& Attr(                                                                                          \
       std::string name, std::string description, AttributeProto::AttributeType type, const TypeName& defaultValue); \
   /* non-STL wrapper to reduce binary size */                                                                       \
-  OpSchema& Attr(                                                                                                   \
+  ONNX_API OpSchema& Attr(                                                                                          \
       const char* name, const char* description, AttributeProto::AttributeType type, const TypeName& defaultValue); \
-  OpSchema& Attr(                                                                                                   \
+  ONNX_API OpSchema& Attr(                                                                                          \
       std::string name,                                                                                             \
       std::string description,                                                                                      \
       AttributeProto::AttributeType type,                                                                           \
@@ -461,7 +461,7 @@ class OpSchema final {
   ATTR_SETTER_WITH_DEFAULT_VALUE(float)
   ATTR_SETTER_WITH_DEFAULT_VALUE(std::string)
   // Overload for const char* default value to prevent implicit conversion to bool
-  OpSchema&
+  ONNX_API OpSchema&
   Attr(const char* name, const char* description, AttributeProto::AttributeType type, const char* defaultValue);
   ATTR_SETTER_WITH_DEFAULT_VALUE(TensorProto)
   ATTR_SETTER_WITH_DEFAULT_VALUE(GraphProto)
@@ -1240,7 +1240,7 @@ class OpSchemaRegistry final : public ISchemaRegistry {
    */
   ONNX_API static OpName_Domain_Version_Schema_Map& GetMapWithoutEnsuringRegistration();
   ONNX_API static OpName_Domain_Version_Schema_Map& map();
-  static int loaded_schema_version;
+  ONNX_API static int loaded_schema_version;
 
  public:
   static std::vector<OpSchema> get_all_schemas_with_history() {
@@ -1373,7 +1373,7 @@ class DbgOperatorSetTracker {
   ONNX_OPERATOR_SET_SCHEMA_CLASS_NAME(OnnxPreview, ver, name)
 
 // Helper function
-size_t ReplaceAll(std::string& s, const char* from, const char* to);
+ONNX_API size_t ReplaceAll(std::string& s, const char* from, const char* to);
 
 // Legacy macros to register schema at static initialization
 #define ONNX_OPERATOR_SCHEMA(name) ONNX_OPERATOR_SCHEMA_UNIQ_HELPER(__COUNTER__, name)
diff --git a/onnx/defs/shape_inference.h b/onnx/defs/shape_inference.h
index eba2024..2f61ec5 100644
--- a/onnx/defs/shape_inference.h
+++ b/onnx/defs/shape_inference.h
@@ -62,7 +62,7 @@ class GraphInferencer {
 
 // Exception class used for handling errors in type and shape inference
 
-class InferenceError final : public std::runtime_error {
+class ONNX_API InferenceError final : public std::runtime_error {
  public:
   using std::runtime_error::runtime_error;
 
@@ -815,9 +815,9 @@ inline void mergeInDimensionInfo(
   }
 }
 
-void mergeInShapeInfo(const TensorShapeProto& source_shape, TypeProto_Tensor& target_type);
+ONNX_API void mergeInShapeInfo(const TensorShapeProto& source_shape, TypeProto_Tensor& target_type);
 
-void mergeInShapeInfo(const TensorShapeProto& source_shape, TypeProto_SparseTensor& target_type);
+ONNX_API void mergeInShapeInfo(const TensorShapeProto& source_shape, TypeProto_SparseTensor& target_type);
 
 /*
 Merge the shape information from two TypeProto_Tensor instances.
@@ -830,9 +830,9 @@ If both have shape information:
 - Prefer target param over source param if mismatched.
 Fail if there are mismatches in number of dimensions or dimension values.
 */
-void mergeInShapeInfo(const TypeProto_Tensor& source, TypeProto_Tensor& target);
+ONNX_API void mergeInShapeInfo(const TypeProto_Tensor& source, TypeProto_Tensor& target);
 
-void mergeInShapeInfo(const TypeProto_SparseTensor& source, TypeProto_SparseTensor& target);
+ONNX_API void mergeInShapeInfo(const TypeProto_SparseTensor& source, TypeProto_SparseTensor& target);
 
 // Return a copy of a type, with a specified dimension removed from its shape.
 inline TypeProto RemoveIthDimensionFromShape(const TypeProto& proto, int removed_dim) {
@@ -996,9 +996,9 @@ inline void unifyDim(Dim& dim, int64_t value) {
 //    input1 shape: (2, 3, 4, 'x')
 //    input2 shape: (2, 3, 4)
 //    output shape: None
-void UnionShapeInfo(const TensorShapeProto& source_shape, TypeProto_Tensor& target_type);
+ONNX_API void UnionShapeInfo(const TensorShapeProto& source_shape, TypeProto_Tensor& target_type);
 
-void UnionShapeInfo(const TensorShapeProto& source_shape, TypeProto_SparseTensor& target_type);
+ONNX_API void UnionShapeInfo(const TensorShapeProto& source_shape, TypeProto_SparseTensor& target_type);
 
 // target-type = Union (target-type, source-type)
 // target and source are required to have the same type.
diff --git a/onnx/defs/tensor_proto_util.cc b/onnx/defs/tensor_proto_util.cc
index 35838f4..1d9977d 100644
--- a/onnx/defs/tensor_proto_util.cc
+++ b/onnx/defs/tensor_proto_util.cc
@@ -35,8 +35,8 @@ int64_t RawDataElementCount(const TensorProto& tensor, size_t element_size, bool
 }
 
 #define DEFINE_TO_TENSOR_ONE(type, enumType, field) \
-  template <>                                       \
-  TensorProto ToTensor<type>(const type& value) {   \
+  template <>                                                \
+  ONNX_API TensorProto ToTensor<type>(const type& value) {   \
     TensorProto t;                                  \
     t.set_data_type(enumType);                      \
     t.add_##field##_data(value);                    \
@@ -44,8 +44,8 @@ int64_t RawDataElementCount(const TensorProto& tensor, size_t element_size, bool
   }
 
 #define DEFINE_TO_TENSOR_LIST(type, enumType, field)            \
-  template <>                                                   \
-  TensorProto ToTensor<type>(const std::vector<type>& values) { \
+  template <>                                                            \
+  ONNX_API TensorProto ToTensor<type>(const std::vector<type>& values) { \
     TensorProto t;                                              \
     t.clear_##field##_data();                                   \
     t.set_data_type(enumType);                                  \
@@ -57,7 +57,7 @@ int64_t RawDataElementCount(const TensorProto& tensor, size_t element_size, bool
 
 #define DEFINE_PARSE_DATA(type, typed_data_fetch, tensorproto_datatype)                                            \
   template <>                                                                                                      \
-  std::vector<type> ParseData(const TensorProto* tensor_proto) {                                                   \
+  ONNX_API std::vector<type> ParseData(const TensorProto* tensor_proto) {                                          \
     if (!tensor_proto->has_data_type() || tensor_proto->data_type() == TensorProto_DataType_UNDEFINED) {           \
       fail_shape_inference("The type of tensor: ", tensor_proto->name(), " is undefined so it cannot be parsed."); \
     } else if (tensor_proto->data_type() != (tensorproto_datatype)) {                                              \
diff --git a/onnx/defs/tensor_proto_util.h b/onnx/defs/tensor_proto_util.h
index 89f083b..2048dd0 100644
--- a/onnx/defs/tensor_proto_util.h
+++ b/onnx/defs/tensor_proto_util.h
@@ -22,7 +22,7 @@ template <typename T>
 ONNX_API TensorProto ToTensor(const std::vector<T>& values);
 
 template <typename T>
-std::vector<T> ParseData(const TensorProto* tensor_proto);
+ONNX_API std::vector<T> ParseData(const TensorProto* tensor_proto);
 
 // Elements that dims implies, checked against raw_data's length. With exact_fit
 // the two must agree exactly; otherwise raw_data may hold trailing bytes, which
diff --git a/onnx/defs/tensor_util.cc b/onnx/defs/tensor_util.cc
index cdc24be..0da0fba 100644
--- a/onnx/defs/tensor_util.cc
+++ b/onnx/defs/tensor_util.cc
@@ -14,7 +14,7 @@ namespace ONNX_NAMESPACE {
 
 #define DEFINE_PARSE_DATA(type, typed_data_fetch)                                             \
   template <>                                                                                 \
-  std::vector<type> ParseData(const Tensor* tensor) {                                         \
+  ONNX_API std::vector<type> ParseData(const Tensor* tensor) {                                \
     std::vector<type> res;                                                                    \
     if (!tensor->is_raw_data()) {                                                             \
       const auto& data = tensor->typed_data_fetch();                                          \
diff --git a/onnx/defs/tensor_util.h b/onnx/defs/tensor_util.h
index 7bcf76f..ca52d0c 100644
--- a/onnx/defs/tensor_util.h
+++ b/onnx/defs/tensor_util.h
@@ -11,6 +11,6 @@
 namespace ONNX_NAMESPACE {
 
 template <typename T>
-std::vector<T> ParseData(const Tensor* tensor);
+ONNX_API std::vector<T> ParseData(const Tensor* tensor);
 
 } // namespace ONNX_NAMESPACE
diff --git a/onnx/inliner/inliner.h b/onnx/inliner/inliner.h
index 5856708..854ab0d 100644
--- a/onnx/inliner/inliner.h
+++ b/onnx/inliner/inliner.h
@@ -36,7 +36,7 @@ class FunctionIdSet {
 
   // Creates a set representing the elements in the given vector, if invert is false.
   // Otherwise, creates a set representing elements not in the given vector.
-  static std::unique_ptr<FunctionIdSet> Create(FunctionIdVector&& function_ids, bool invert = false);
+  ONNX_API static std::unique_ptr<FunctionIdSet> Create(FunctionIdVector&& function_ids, bool invert = false);
 };
 
 /**
@@ -47,7 +47,7 @@ class FunctionIdSet {
  *        the default schema registry is used.
  * @note Only call-sites in the main graph are inlined.
  */
-void InlineSelectedFunctions(ModelProto& model, const FunctionIdSet& to_inline, const ISchemaRegistry* schema_registry);
+ONNX_API void InlineSelectedFunctions(ModelProto& model, const FunctionIdSet& to_inline, const ISchemaRegistry* schema_registry);
 
 /**
  * @brief Inlines the model-local functions in the given model that are in the given set.
@@ -62,7 +62,7 @@ void InlineSelectedFunctions(ModelProto& model, const FunctionIdSet& to_inline,
  * @note This function does not perform schema-defined function inlining. For schema-defined
  *       function inlining, use InlineSelectedFunctions instead.
  */
-void InlineSelectedLocalFunctions(ModelProto& model, const FunctionIdSet& to_inline);
+ONNX_API void InlineSelectedLocalFunctions(ModelProto& model, const FunctionIdSet& to_inline);
 
 /**
  * @brief Inlines the model-local functions in the given model that are in the given set.
@@ -70,14 +70,14 @@ void InlineSelectedLocalFunctions(ModelProto& model, const FunctionIdSet& to_inl
  * to avoid confusion with the overloaded version of InlineSelectedFunctions that
  * inlines schema-defined functions as well.
  */
-void InlineSelectedFunctions(ModelProto& model, const FunctionIdSet& to_inline);
+ONNX_API void InlineSelectedFunctions(ModelProto& model, const FunctionIdSet& to_inline);
 
 // Inlines all model-local functions in the given model. This supports version
 // conversion, an advanced feature that is not enabled by default. When enabled,
 // the inliner will attempt to convert the version of the inlined function to
 // match the version of the model. If not enabled, the inliner will only inline
 // functions that use opset versions that are compatible with the model.
-void InlineLocalFunctions(ModelProto& model, bool convert_version = false);
+ONNX_API void InlineLocalFunctions(ModelProto& model, bool convert_version = false);
 
 /**
  * @brief A utility class for renaming variables during graph inlining operations.
diff --git a/onnx/shape_inference/implementation.h b/onnx/shape_inference/implementation.h
index 08ab521..cebac47 100644
--- a/onnx/shape_inference/implementation.h
+++ b/onnx/shape_inference/implementation.h
@@ -145,7 +145,7 @@ struct GraphInferenceContext {
   const int64_t ir_version;
 };
 
-class GraphInferencerImpl : public GraphInferencer {
+class ONNX_API GraphInferencerImpl : public GraphInferencer {
  public:
   GraphInferencerImpl(GraphProto& g, GraphInferenceContext& context) : g_{&g}, context_{&context} {}
   GraphInferencerImpl(GraphProto& g, GraphInferenceContext& context, const ShapeInferenceOptions& options)
@@ -550,7 +550,7 @@ ONNX_API void InferShapes(
 /// ModelLocalFunctionsMap is a map of function id -> model local function proto
 /// All the ONNX helper utilities expect the function id == <function_proto.domain>:<function_proto.name>
 ///
-void InferShapeForFunctionNode(
+ONNX_API void InferShapeForFunctionNode(
     const FunctionProto& func,
     const ISchemaRegistry* schema_registry,
     InferenceContext& ctx,
@@ -563,7 +563,7 @@ void InferShapeForFunctionNode(
 /// ModelLocalFunctionsMap is a map of function id -> model local function proto
 /// All the ONNX helper utilities expect the function id == <function_proto.domain>:<function_proto.name>
 ///
-void InferShapeForFunctionNode(
+ONNX_API void InferShapeForFunctionNode(
     const FunctionProto& func_proto,
     const std::unordered_map<std::string, int>& func_opset_imports,
     const ISchemaRegistry* schema_registry,
@@ -587,7 +587,7 @@ void InferShapeForFunctionNode(
 /// represent an input that is present but whose type is unknown: such an input is
 /// indistinguishable, from this API's perspective, from one that is simply absent.
 ///
-std::vector<TypeProto> InferFunctionOutputTypes(
+ONNX_API std::vector<TypeProto> InferFunctionOutputTypes(
     const FunctionProto& function_proto,
     const std::vector<TypeProto>& input_types,
     const std::vector<AttributeProto>& attributes);
