{% set name = "onnx" %}
{% set version = "1.1.0" %}
{% set sha256 = "7cd7cf2338ccb29a41e7074d863e95af786fdee9e085ffdfedc71a1154ab4af0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/onnx/onnx/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script:
    - export ONNX_BINARY_BUILD=1  # [unix]
    - set "ONNX_BINARY_BUILD=1"   # [win]
    - python -m pip install --no-deps --ignore-installed .
  entry_points:
    - check-model = onnx.bin.checker:check_model
    - check-node = onnx.bin.checker:check_node
    - backend-test-tools = onnx.backend.test.cmd_tools:main

requirements:
  build:
    - python
    - setuptools
    - toolchain
    - protobuf 3.5.*
    - pybind11  # submodule isn't included in github source file
    - pytest-runner
    - ninja
  run:
    - python
    - protobuf 3.5.*
    - numpy

test:
  imports:
    - onnx
  commands:
    - conda inspect linkages -p $PREFIX {{ name }}  # [unix]
    - conda inspect objects -p $PREFIX {{ name }}   # [osx]

about:
  home: https://github.com/onnx/onnx/
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Open Neural Network Exchange library
  description: |
    Open Neural Network Exchange (ONNX) is the first step toward an open
    ecosystem that empowers AI developers to choose the right tools as their
    project evolves. ONNX provides an open source format for AI models. It
    defines an extensible computation graph model, as well as definitions of
    built-in operators and standard data types. Initially we focus on the
    capabilities needed for inferencing (evaluation).

extra:
  recipe-maintainers:
    - ezyang
    - dougalsutherland
